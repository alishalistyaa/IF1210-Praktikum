module PersamaanKuadrat where

    -- Definisi dan Spesifikasi
    persamaanKuadrat :: Int -> Int -> Int -> Int -> Int
    {-membuat persamaan kuadrat-}

    -- Realisasi
    persamaanKuadrat a b c x = a * (x ^2) + b * x + c

    -- Contoh Aplikasi
    -- persamaanKuadrat 1 1 1 1